import React from 'react';

const Input = () => {
    return (
        <div>
            <form>
                <input type="text" placeholder="tapping text search"/>
            </form>
        </div>
        )
}

export default Input;